import SplashLoader from "@/components/brand/SplashLoader";

/** Route-level loading state: the brand-kit 2a splash, full screen. */
export default function Loading() {
  return <SplashLoader fullScreen />;
}
